{
  "$provenance": {
    "spec": "specs/mobility/v1.0.0/openapi.json",
    "operation": "listDevices (GET /v1/mobility/workspaces/{workspaceID}/devices)",
    "pointer": "#/components/schemas/ErrorResponse",
    "note": "FR-40 branch 1 of 4. The message contains \"app scope\", the first pattern the hint builder tests, so the hint must name the missing `mobility` scope specifically rather than enumerate all three causes."
  },
  "body": {
    "code": "forbidden",
    "httpStatusCode": 403,
    "message": "permission denied for app: mobility app scope not granted to this key",
    "traceId": "7f3a1c2b-d4e5-4f6a-8b9c-0d1e2f3a4b5c"
  }
}
