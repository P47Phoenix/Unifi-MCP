{
  "$provenance": {
    "spec": "specs/protect/v7.1.87/openapi.json",
    "operation": "default response of GET /v1/cameras",
    "pointer": "#/components/schemas/genericError",
    "note": "The plain required shape: error + name only. `name` is the upstreamCode source for this service — the opposite convention to the other three, where `code` is."
  },
  "body": {
    "error": "Invalid camera id",
    "name": "BAD_REQUEST"
  }
}
