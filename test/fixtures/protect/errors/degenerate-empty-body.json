{
  "$provenance": {
    "spec": "specs/protect/v7.1.87/openapi.json",
    "operation": "default response of GET /v1/cameras",
    "pointer": "#/components/schemas/genericError",
    "note": "Degenerate: an empty object. upstreamCode, correlationId and httpStatus-from-body are all null, and the message must fall back to the status-derived sentence."
  },
  "body": {}
}
