{
  "$provenance": {
    "spec": "specs/protect/v7.1.87/openapi.json",
    "operation": "default response of GET /v1/cameras",
    "pointer": "#/components/schemas/genericError",
    "note": "Required shape. Pair with a Retry-After header for FR-26; the body carries no retry hint of its own."
  },
  "body": {
    "error": "Too many requests",
    "name": "RATE_LIMITED"
  }
}
