{
  "$provenance": {
    "spec": "specs/site-manager/v1.0.0/openapi.json",
    "operation": "listSites (GET /v1/sites)",
    "pointer": "#/paths/~1v1~1sites/get/responses/401/content/application~1json/schema",
    "note": "NOT observed in the spec: no example anywhere emits `BAD_REQUEST`. Authored to exercise the one SITE_MANAGER_CATEGORIES key that authentic data never reaches, so a regression that drops it is still visible."
  },
  "body": {
    "code": "BAD_REQUEST",
    "httpStatusCode": 400,
    "message": "malformed request body",
    "traceId": "a7dc15e0eb4527142d7823515b15f87d"
  }
}
