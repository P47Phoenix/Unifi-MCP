{
  "$provenance": {
    "spec": "specs/site-manager/v1.0.0/openapi.json",
    "operation": "listSites (GET /v1/sites)",
    "pointer": "#/paths/~1v1~1sites/get/responses/401/content/application~1json/schema",
    "note": "Spec-authentic, verbatim from the 502 example. BAD_GATEWAY is the one table key that maps to a differently-named category (server_error); the lowercase authentic code reaches the same answer via the status map."
  },
  "body": {
    "code": "bad_gateway",
    "httpStatusCode": 502,
    "message": "bad gateway",
    "traceId": "a7dc15e0eb4527142d7823515b15f87d"
  }
}
